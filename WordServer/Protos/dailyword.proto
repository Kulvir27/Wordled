syntax = "proto3";

option csharp_namespace = "WordServer.Protos";

service DailyWord {
	//rpc GetWord() returns (Word); // rpc to return a word from wordle.json file
	//rpc ValidateWord() returns ValidWord; // rpc to validate word
}
